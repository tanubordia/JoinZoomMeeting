#!/bin/bash
    pip install virtualCamera
    pip install pyautogui
    pip install webbrowser
    pip install datetime
    pip install datetime
    pip install click
    pip install os
    pip install schedule
    pip install cv2
    pip install pyvirtualcam

    pip install numpy